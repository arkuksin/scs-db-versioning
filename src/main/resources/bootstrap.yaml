spring:
  application:
    name: post-service
  cloud:
    kubernetes:
      config:
        name: post-configmap
        enable-api: true
        enabled: true
      secrets:
        name: post-secret
        enable-api: true
        enabled: true